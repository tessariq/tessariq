---
id: T-113
title: Cover interrupt-driven cleanup in e2e
status: completed
priority: high
spec_ref: specs/tessariq-v0.1.0.md#lifecycle-rules
dependencies: []
updated_at: "2026-07-30T14:43:12Z"
---

## Summary

No e2e case interrupts a run in progress. The manual plan requires that a `Ctrl-C` on `tessariq run` cleans up the worktree, the agent container, the Squid container, and the per-run network, and that the recorded terminal status agrees with the CLI exit code.

## Motivation

BUG-048 was this defect: interrupted runs were left indefinitely `running` with leaked resources. The fix shipped, but interrupt handling is the single most leak-prone path in the lifecycle and has no automated coverage at all. Leaks here accumulate silently on a user's machine.

## Acceptance Criteria

- An e2e case starts a run in proxy mode, sends SIGINT to the CLI process mid-run, and asserts a non-zero exit.
- The case asserts no leftover agent container, no leftover Squid container, and no leftover per-run Docker network.
- The case asserts the worktree is removed and `git worktree list` has no stale entry.
- The case asserts `status.json` records a terminal, non-`running` state consistent with the CLI exit code (BUG-048 regression).
- The case asserts container removal destroys no evidence: after the run, the full terminal evidence set is present and non-empty, and `run.log` still holds the agent output that was streamed out of the container while it was alive. (Amended 2026-07-30: the original wording said "terminal evidence was written before container removal", which is inverted relative to the shipped design — the runner deliberately runs container cleanup before writing the final `status.json`, see the comment at `internal/runner/runner.go` and `TestRunner_CleanupCalledBeforeStatusWrite`.)

## Test Expectations

- Build tag `//go:build e2e`; new cases live alongside the existing suite in `cmd/tessariq/`.
- Use `containers.StartRunEnv` and the default `runTessariq` helper (which passes `--no-update-agent`); add a `Start*` helper to `internal/testutil/containers/` if a new collaborator is genuinely required.
- No host-tool guards and no local fake binaries or servers: the container supplies every runtime dependency.
- Runs on GitHub Actions `ubuntu-latest` in the existing `e2e` CI job with no secrets, no credentials, and no billed model calls.

## TDD Plan

1. RED: add the SIGINT case asserting terminal status and zero leaked resources.
2. GREEN: no production change expected.
3. VERIFY: confirm the case fails when signal handling is bypassed so the run stays `running`.

## Notes

- Covers manual plan case T4.22.
- Signal the CLI process inside the container rather than the test process; the fake agent should sleep long enough to make the interrupt window deterministic.
- Enumerate Docker resources by the deterministic `tessariq-<run_id>` name and the per-run network name so the assertions cannot pass vacuously.

## Implementation Notes

- 2026-07-30T14:43:06Z: verification pass
- 2026-07-30T14:43:12Z: Added TestE2E_InterruptedProxyRunCleansUpAndRecordsTerminalState (cmd/tessariq/run_e2e_test.go) covering manual case T4.22 and the BUG-048 regression. Evidence: `go test -tags=e2e ./... -count=1 -timeout=15m` green (cmd/tessariq 264.571s); `gofmt -l .`, `go vet ./...`, `go test ./...`, `go test -tags=integration ./...` all clean. Local verify report under the gitignored verify artifacts dir for this task id.
