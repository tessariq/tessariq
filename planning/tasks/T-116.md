---
id: T-116
title: Cover post-run resource hygiene in e2e
status: completed
priority: medium
spec_ref: specs/tessariq-v0.1.0.md#workspace-guarantees
dependencies: []
updated_at: "2026-07-31T11:52:19Z"
---

## Summary

Cleanup is covered for the failure path (`TestE2E_FailedRunCleansUpWorktree`) but not for a successful run. The manual plan sweeps for leftover containers, leftover networks, a removed worktree, no stale `git worktree` entries, and a removed per-run runtime-state directory.

## Motivation

Success-path leaks are the worst kind: nothing looks wrong, and the user accumulates containers, networks, worktrees, and copied credentials under `~/.tessariq/runtime-state/` over time. The runtime-state directory holds a copy of host auth, so failing to remove it is also a security concern.

## Acceptance Criteria

- An e2e case completes a successful run and asserts no container matching `tessariq-<run_id>` remains.
- The case asserts no per-run Docker network remains.
- The case asserts the worktree directory is gone and `git worktree list` has no stale entry.
- The case asserts the per-run directory under `~/.tessariq/runtime-state/` is removed, including the copied auth file.
- The same sweep is asserted after a timed-out run, so cleanup is proven on more than the happy path.

## Test Expectations

- Build tag `//go:build e2e`; new cases live alongside the existing suite in `cmd/tessariq/`.
- Use `containers.StartRunEnv` and the default `runTessariq` helper (which passes `--no-update-agent`); add a `Start*` helper to `internal/testutil/containers/` if a new collaborator is genuinely required.
- No host-tool guards and no local fake binaries or servers: the container supplies every runtime dependency.
- Runs on GitHub Actions `ubuntu-latest` in the existing `e2e` CI job with no secrets, no credentials, and no billed model calls.

## TDD Plan

1. RED: add the success-path hygiene sweep asserting every resource class is gone.
2. RED: repeat the sweep for a timed-out run.
3. GREEN: no production change expected.
4. VERIFY: confirm the sweep fails when a cleanup step is deliberately skipped.

## Notes

- Covers manual plan case T1.6.
- Assert on the specific deterministic names rather than counting resources, so an unrelated container on the runner cannot mask a leak or cause a false failure.
- Removal of the runtime-state copy of host auth is the highest-value assertion here.

## Implementation Notes

- 2026-07-31T11:51:09Z: verification pass
- 2026-07-31T11:52:19Z: Added e2e post-run resource hygiene coverage: TestE2E_SuccessfulRunLeavesNoResources and TestE2E_TimedOutRunLeavesNoResources in cmd/tessariq/run_e2e_test.go, backed by requireRunResourcesCleanedUp (agent container, Squid container, per-run network, runtime-state dir + copied auth file) and requireWorktreeRemoved/requireWorktreeRetained. Consolidated the duplicated worktree sweep in TestE2E_InterruptedProxyRunCleansUpAndRecordsTerminalState onto the same helper. Non-vacuity proven by three deliberate production regressions, each reverted: disabling runtimeState.Cleanup, workspace.Cleanup, and the runner container cleaner each failed the sweep with the matching assertion. Manual test run in container mode confirmed all four sweep classes by raw docker/git/find inspection on both a successful and a timed-out run, plus a synthetic-leak injection proving the inspection commands surface a leak; verdict pass-with-fixes, the one fix being to the ephemeral manual harness image name, not to product code. Test-only change; no production code modified. AC deviation recorded as follow-up T-120: worktree removal does not hold on the success path since run.go:411 retains it as the promotable workspace.
